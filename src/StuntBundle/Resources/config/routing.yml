# Default
default:
    path: /
    defaults: { _controller: "StuntBundle:Default:index" }
    options:
        expose: true

# Security
registration:
    path: /register
    defaults: { _controller: "StuntBundle:Security:registration" }

login:
    path: /login
    defaults: { _controller: "StuntBundle:Security:login" }

logout:
    path: /logout

# User
accept_pd_processing:
    path: /accept_pd_processing
    defaults: { _controller: "StuntBundle:User:acceptPdProcessing" }

# Congregant
congregant_index:
    path: /congregant
    defaults: { _controller: "StuntBundle:Congregant:index" }